if [[ "$PBS_O_WORKDIR" == "" ]] ; then
  echo -e "\e[1;33m$(screen -ls)$(tmux list-sessions)"
  if [[ -x `which qstat 2>/dev/null` ]] ; then
    echo -en "$(ql)"
  elif [[ -x `which squeue 2> /dev/null` ]] ; then
    echo -en "$(squeue -u $USER)"
  fi
  echo -e "\e[0m"
fi
